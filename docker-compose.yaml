services:
  backend:
    container_name: "backend"
    build:
      context: ./backend
    ports:
      - "8000:8000"

  frontend:
    container_name: "frontend"
    build:
      context: ./frontend
    ports:
      - "3000:80"